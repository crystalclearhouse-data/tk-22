Agents may touch:

- files under `automation/` and `data/` subfolders as explicitly allowed

Agents must never touch:

- secrets or credentials
- files at repo root except `docs/` configuration
- source files under `frontend/` or `backend/` unless explicitly permitted
